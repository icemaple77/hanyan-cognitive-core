{
  "id": "hcc-memory",
  "name": "HCC Memory",
  "kind": "memory",
  "description": "Bridges OpenClaw memory to HCC (Hanyan Cognitive Core) REST API for hybrid search, recall, and durable storage",
  "activation": { "onStartup": true },
  "contracts": {
    "tools": ["memory_search", "memory_get"]
  },
  "toolMetadata": {
    "memory_search": { "replaySafe": true },
    "memory_get": { "replaySafe": true }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "baseUrl": {
        "type": "string",
        "description": "HCC gateway base URL, e.g. http://100.66.103.69:8000"
      },
      "userId": {
        "type": "string",
        "description": "HCC user_id to scope memories under"
      },
      "agentId": {
        "type": "string",
        "description": "HCC agent_id to scope memories under"
      },
      "sessionRecallEnabled": {
        "type": "boolean",
        "description": "Enable session_start auto-recall + prependSystemContext injection (default true)"
      },
      "sessionRecallLimit": {
        "type": "integer",
        "minimum": 1,
        "description": "Max auto-recalled memories injected at session start (default 5)"
      },
      "emotionEnabled": {
        "type": "boolean",
        "description": "Enable emotion warm-start at session start and emotion write-back at session end (default true)"
      },
      "fetchTimeoutMs": {
        "type": "integer",
        "minimum": 500,
        "description": "Abort timeout for HCC API calls, ms (default 8000)"
      }
    }
  }
}
